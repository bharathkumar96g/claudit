# Copy to .env and adjust. .env is gitignored.

# Where Claude Code writes transcripts (one subdir per project, *.jsonl inside)
CLAUDIT_TRANSCRIPTS_DIR=~/.claude/projects

# Local DuckDB file. Stores fingerprints + locations only, never raw secret values.
CLAUDIT_DB_PATH=data/claudit.duckdb
